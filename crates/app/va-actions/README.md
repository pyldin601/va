# va-actions

Lists supported commands and executes tool calls for server-side actions.

## Endpoints

- `GET /commands` — list supported commands with examples and AI instructions.
- `POST /execute` — execute a command from the list.

## Run locally

```bash
cargo run -p va-actions
```
